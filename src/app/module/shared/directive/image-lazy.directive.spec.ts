import { ImageLazyDirective } from './image-lazy.directive';

describe('ImageLazyDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageLazyDirective();
    expect(directive).toBeTruthy();
  });
});
