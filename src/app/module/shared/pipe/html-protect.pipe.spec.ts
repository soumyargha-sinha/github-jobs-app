import { HtmlProtectPipe } from './html-protect.pipe';

describe('HtmlProtectPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlProtectPipe();
    expect(pipe).toBeTruthy();
  });
});
